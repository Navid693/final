حتماً! بیایید چک‌لیست بک‌اند را با ظاهری بهتر، خواناتر و با استفاده از ایموجی و تیک بازنویسی کنیم تا هم شیک‌تر باشد و هم دنبال کردنش راحت‌تر.

---

## 🗺️ نقشه راه و وظایف بک‌اند (Backend) - پروژه SCU Remote Desktop 🗺️

این لیست، وظایف اصلی و پروتکل‌های ارتباطی مورد نیاز برای پیاده‌سازی سمت سرور پروژه است.

### 🔑 ۱. احراز هویت و API پایه (HTTP REST)

* ✅ **پیاده‌سازی اندپوینت `POST /api/login`:**
    * ورودی: `{"username", "password"}`
    * منطق: بررسی اعتبار کاربر در دیتابیس.
    * خروجی: `{"success", "token"}` (JWT) یا `{"success", "message"}`.
* ✅ **(اختیاری) پیاده‌سازی اندپوینت `POST /api/register`:**
    * ورودی: `{"username", "password"}`
    * منطق: ایجاد کاربر جدید (بررسی عدم تکرار).
    * خروجی: `{"success", "message"}`.
* ✅ **پیاده‌سازی دیتابیس کاربران:** ذخیره امن اطلاعات ورود.
* ✅ **پیاده‌سازی مکانیزم JWT:** تولید و اعتبارسنجی توکن‌ها.

### 🔌 ۲. اتصال WebSocket و ثبت کاربر

* ✅ **پیاده‌سازی اندپوینت WebSocket:** در مسیر `/ws` (یا مسیر توافقی).
* ✅ **پشتیبانی از `wss://` (اکیداً توصیه می‌شود) و/یا `ws://`.**
* ✅ **مدیریت اتصال WS:**
    * دریافت پیام `{"type": "register", "username", "token"}`.
    * **اعتبارسنجی توکن JWT** دریافت شده.
    * ذخیره کانکشن کاربر معتبر در وضعیت داخلی سرور.
    * پاسخ با `{"type": "register_response", "success", "message"}`.
* ✅ **مدیریت قطع اتصال WS:** پاک کردن وضعیت کاربر، اطلاع به Peer، ارسال `users_list` جدید.

### 👥 ۳. مدیریت کاربران و اتصال Peer-to-Peer

* ✅ **نگهداری وضعیت:** لیست کاربران آنلاین (Username->Connection) و لینک‌های Peer فعال (UserA <-> UserB).
* ✅ **ارسال لیست کاربران:** ارسال `{"type": "users_list", "users": [...]}` به *همه* پس از اتصال/قطع کاربر.
* ✅ **مدیریت درخواست اتصال (Peer Request):**
    * دریافت `{"type": "request_view", "target_uid", "sender_uid"}` از A.
    * ارسال `{"type": "connection_request", "sender_uid": "A"}` به B (اگر آزاد بود).
* ✅ **مدیریت پاسخ اتصال (Peer Response):**
    * دریافت `{"type": "view_response", "target_uid": "A", "allowed"}` از B.
    * ارسال `{"type": "connection_response", "sender_uid": "B", "allowed"}` به A.
    * **برقراری لینک Peer** بین A و B در سرور (اگر `allowed: true`).
* ✅ **مدیریت قطع اتصال Peer:**
    * دریافت `{"type": "disconnect_peer"}` از A.
    * ارسال `{"type": "peer_disconnected", "sender_uid": "A", "reason": "..."}` به B.
    * حذف لینک Peer.
    * مدیریت قطع اتصال ناگهانی و اطلاع‌رسانی مشابه.

### 💬 ۴. انتقال پیام‌های چت

* ✅ **دریافت `{"type": "chat", "text"}` از A.**
* ✅ **ارسال `{"type": "chat", "sender_uid": "A", "text"}` به Peer متصل (B).**

### 💻🖱️ ۵. انتقال داده‌های کنترل از راه دور

* ✅ **دریافت `{"type": "input_event", "event": {...}}` از Controller (A).**
* ✅ **بررسی دسترسی:** آیا Sharer (B) به A اجازه کنترل داده؟ (طبق وضعیت ذخیره شده در بخش ۶).
* ✅ **اگر مجاز بود:** ارسال `{"type": "input_event", "sender_uid": "A", "event"}` به Sharer (B).
* ✅ **دریافت داده باینری** (فریم تصویر/موس) از Sharer (A).
* ✅ **عیناً ارسال همان داده باینری** به Controller متصل (B) (عملکرد شبیه Pipe).

### ✅🔒 ۶. مدیریت دسترسی کنترل (Permissions)

* ✅ **نگهداری وضعیت داخلی دسترسی‌ها:** (Sharer -> Controller -> Allowed).
* ✅ **دریافت `{"type": "input_permission_update", "target_uid": "B", "allowed"}` از Sharer (A).**
* ✅ **به‌روزرسانی وضعیت دسترسی ذخیره شده در سرور.**
* ✅ **ارسال `{"type": "input_permission_update", "sender_uid": "A", "allowed"}` به Controller (B).**

### 📝 ۷. لاگینگ و تاریخچه

* ✅ **پیاده‌سازی لاگینگ جامع رویدادهای سرور.**
* ✅ **(اختیاری) ذخیره تاریخچه اتصالات و/یا چت‌ها در دیتابیس.**

### 🛡️ ۸. امنیت

* ✅ **(مهم) اعتبارسنجی JWT در پیام `register` WebSocket.**
* ✅ **استفاده از `WSS` برای رمزنگاری.**
* ✅ **اعتبارسنجی فرمت پیام‌های دریافتی.**
* ✅ **(اختیاری) Rate Limiting.**

### ⚠️ ۹. مدیریت خطا

* ✅ **ارسال پیام `{"type": "error", "message": "..."}` به کلاینت(ها) برای خطاهای عملیاتی.**
* ✅ **لاگ کردن دقیق خطاهای داخلی سرور.**

---

امیدوارم این نسخه مرتب‌تر و خواناتر باشد و به دوستتان در پیاده‌سازی بک‌اند کمک کند!